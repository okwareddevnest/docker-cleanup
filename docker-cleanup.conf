# Docker Cleanup Configuration
# This file contains the default settings for docker-cleanup

# Cleanup mode options:
# - "all": Clean up all containers
# - "stopped": Only clean up stopped containers
# - "older:X": Clean up containers older than X days
CLEANUP_MODE="all" 